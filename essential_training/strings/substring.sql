-- this shows how to
-- subset strings in SQL
SELECT SUBSTR('This string', 6); -- takes the string, and the start character -> 'string'
SELECT SUBSTR('This string', 6, 3); -- takes also the end length of the subset -> 'str'
